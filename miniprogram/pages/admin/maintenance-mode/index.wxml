<view class="page">
  <view class="page-intro">系统配置：工作时间、服务模式、协议版本与运营数据导出。</view>

  <view wx:if="{{settings}}" class="section">
    <view class="surface-section">
      <view class="list-card">
        <view class="row-between settings-row">
          <view class="settings-copy">
            <view class="nav-title">服务模式</view>
            <view class="page-subtitle">{{settings.serviceMode === 'maintenance' ? '维护中' : settings.serviceMode === 'rule_migrating' ? '规则迁移中' : '正常'}}</view>
          </view>
          <button class="button button--secondary button--compact settings-action" bindtap="toggleServiceMode">{{settings.serviceMode === 'normal' ? '开启维护' : '恢复'}}</button>
        </view>
      </view>

      <view class="list-card">
        <view class="row-between settings-row">
          <view class="settings-copy">
            <view class="nav-title">工作时间</view>
            <view class="page-subtitle">{{settings.openStartHour}}:00 - {{settings.openEndHour}}:00</view>
          </view>
          <button class="button button--secondary button--compact settings-action" bindtap="showForm">修改</button>
        </view>
      </view>

      <view class="list-card">
        <view class="nav-title">协议版本</view>
        <view class="page-subtitle">服务协议：v{{settings.serviceAgreementVersion}} · 隐私政策：v{{settings.privacyPolicyVersion}}</view>
      </view>

      <view class="list-card">
        <view class="nav-title">提前预约天数</view>
        <view class="page-subtitle">{{settings.maxAdvanceDays}} 天</view>
      </view>
    </view>
  </view>

  <view wx:if="{{showForm}}" class="surface-section">
    <view class="section-title">修改工作时间</view>
    <view class="field">
      <view class="field-label">开始时间（时）</view>
      <input class="input" type="number" value="{{form.openStartHour}}" data-field="openStartHour" bindinput="onHourInput" />
    </view>
    <view class="field">
      <view class="field-label">结束时间（时）</view>
      <input class="input" type="number" value="{{form.openEndHour}}" data-field="openEndHour" bindinput="onHourInput" />
    </view>
    <view class="field">
      <view class="field-label">最多提前预约天数</view>
      <input class="input" type="number" value="{{form.maxAdvanceDays}}" data-field="maxAdvanceDays" bindinput="onDaysInput" />
    </view>
    <view class="privacy-actions">
      <button class="button button--secondary" bindtap="hideForm">取消</button>
      <button class="button button--primary" loading="{{loading}}" bindtap="saveHours">保存</button>
    </view>
  </view>

  <view class="section surface-section export-section">
    <view class="row-between settings-row">
      <view class="settings-copy">
        <view class="nav-title">运营数据导出</view>
        <view class="page-subtitle">仅管理员可导出内部运营 JSON，包含课题组与预约人信息；不含 openid、备注、隐私请求和审核日志。</view>
      </view>
      <button class="button button--secondary button--compact settings-action" loading="{{exporting}}" disabled="{{exporting}}" bindtap="exportData">导出</button>
    </view>
  </view>

  <view class="section">
    <view class="section-title">使用统计</view>
    <view class="stat-grid">
      <view class="stat-card">
        <view class="stat-value">{{stats.totalHours || 0}}</view>
        <view class="stat-label">总小时</view>
      </view>
      <view class="stat-card">
        <view class="stat-value">{{stats.byTimeType.workingHours || 0}}</view>
        <view class="stat-label">工作时间</view>
      </view>
      <view class="stat-card">
        <view class="stat-value">{{stats.byTimeType.nonWorkingHours || 0}}</view>
        <view class="stat-label">非工作时间</view>
      </view>
      <view class="stat-card">
        <view class="stat-value">{{stats.monthCount || 0}}</view>
        <view class="stat-label">统计月份</view>
      </view>
    </view>
  </view>

  <view class="section">
    <view class="section-title">月份统计</view>
    <empty-state wx:if="{{stats.byMonth.length === 0}}" title="暂无月份统计" />
    <view wx:if="{{stats.byMonth.length > 0}}" class="surface-section">
      <view wx:for="{{stats.byMonth}}" wx:key="month" class="list-card stat-row">
        <view class="stat-name">{{item.month}}</view>
        <view class="stat-hours">{{item.hours}}h</view>
      </view>
    </view>
  </view>
</view>
