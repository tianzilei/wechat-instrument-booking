page {
  min-height: 100%;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", Arial, sans-serif;
  font-size: var(--font-size-md);
  line-height: var(--line-normal);
}

view,
scroll-view,
button,
input,
textarea {
  box-sizing: border-box;
}

button {
  max-width: 100%;
  min-width: 0;
  margin: 0;
  padding: 0;
  border-radius: var(--radius-md);
  font-size: var(--font-size-md);
  line-height: 1;
  overflow: hidden;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}

button::after {
  border: 0;
}

input,
textarea {
  width: 100%;
  color: var(--color-text);
}

.page {
  min-height: 100vh;
  width: 100%;
  max-width: 960rpx;
  margin: 0 auto;
  padding: 24rpx var(--page-gutter) calc(132rpx + env(safe-area-inset-bottom));
}

.page--flush {
  padding-left: 0;
  padding-right: 0;
}

.page-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
  padding-bottom: 24rpx;
}

.page-title {
  font-size: var(--font-size-lg);
  font-weight: 700;
  line-height: var(--line-tight);
}

.page-subtitle {
  margin-top: 8rpx;
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
  line-height: 1.55;
}

.section {
  margin-top: 24rpx;
}

.section-title {
  margin-bottom: 16rpx;
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
  font-weight: 600;
}

.muted {
  color: var(--color-text-muted);
}

.page-intro {
  margin-bottom: 24rpx;
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
  line-height: 1.55;
}

.safe-bottom {
  padding-bottom: env(safe-area-inset-bottom);
}
